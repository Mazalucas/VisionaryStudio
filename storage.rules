rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/frames/{fileName} {
      allow read, write: if request.auth != null;
    }
    match /projects/{projectId}/frames/{frameId}/generations/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
